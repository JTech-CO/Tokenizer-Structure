# Tokenizer Structure 기능 다양화 로드맵

> 이 문서는 **기획 전용**입니다. 현재 작업 범위에서는 아래 기능을 구현하지 않으며, 디버깅·내용 최신화·회귀 검증이 끝난 기준선 위에서 순서대로 검토합니다.

## 기획 원칙

- 실제 토크나이저 결과와 휴리스틱 폴백을 항상 명확히 구분합니다.
- 가격·컨텍스트·모델 대응 관계에는 기준일과 공식 출처를 함께 표시합니다.
- 원문은 기본적으로 브라우저 밖으로 전송하지 않는 현재의 개인정보 보호 특성을 유지합니다.
- 모델 로드 실패나 일부 데이터 누락은 가짜 숫자로 대체하지 않고, 실패 상태와 재시도 경로를 보여줍니다.
- 키보드, 모바일, 축소 애니메이션 환경을 각 기능의 완료 조건에 포함합니다.

## Gate 0 — 안정화 기준선

상태: 이번 정비에서 완료

- 빠른 모델 전환 시 오래된 비동기 결과가 최신 화면을 덮지 않도록 방지
- byte-level 토큰 사이에 나뉜 UTF-8 문자를 스트리밍 디코드
- 매트릭스 로드 실패를 추정값이 아닌 미사용 셀로 표시
- 가격 티어 계산, 모델·컨텍스트·단가 데이터, 설명 문구 최신화
- 모바일 스크롤, 키보드 포커스, 모달 포커스 복귀·가두기 보완
- 핵심 계산과 정적 자산 정합성에 대한 회귀 테스트 추가

## 공통 기술 부채 — 배포 자산 고정

상태: 후속 기획

- Tailwind Play CDN 의존을 재현 가능한 로컬 생성 CSS로 교체
- 외부 스크립트의 버전·무결성 정책과 Content Security Policy 설계
- CDN 차단·장애 환경에서도 핵심 설명과 오류 상태가 유지되는지 테스트

## Phase 1 — 입력·내보내기·공유

목표: 분석 결과를 다시 사용하고 다른 사람과 같은 화면을 재현할 수 있게 합니다.

범위:

- 줄 번호와 글자·UTF-8 바이트 제한을 제공하는 다중행 편집기
- Token ID, raw token, 표시 문자열을 JSON·CSV로 복사/다운로드
- 입력을 제외한 모델·보기·옵션 상태를 URL로 공유
- 사용자가 명시적으로 선택할 때만 입력까지 공유 링크에 포함

의존성·주의점:

- 긴 URL 제한과 민감한 원문 노출 경고
- 내보내기 스키마 버전과 실제/폴백 엔진 표기
- 빈 입력, 특수문자, 조합형 유니코드 테스트

완료 조건:

- 내보낸 토큰 수와 화면 토큰 수가 일치
- 공유 링크가 새 탭에서 동일한 모델·보기·옵션을 복원
- 모바일과 키보드만으로 모든 내보내기 동작 수행 가능

## Phase 2 — 비교 실험실

목표: 2개 고정 비교를 재현 가능한 2~4개 모델 실험으로 확장합니다.

범위:

- 2~4개 토크나이저 열 추가·제거
- 사용자 정의 문장 묶음과 언어 태그
- 토큰 수, 코드 포인트/토큰, 바이트/토큰, 컨텍스트 점유율 정렬
- 행 필터와 CSV 결과 내보내기

의존성·주의점:

- 동시 다운로드·메모리 상한과 취소 가능한 로딩
- 실패 셀을 순위·평균·색상 계산에서 제외
- 샘플별 상대 비교와 언어 전체 일반화를 구분하는 안내

완료 조건:

- 빠른 모델 교체에서도 각 열에 선택한 모델의 결과만 표시
- 부분 실패 후 성공한 열은 유지되고 실패 열만 재시도
- 동일 입력·모델 조합의 내보내기 결과가 단일 파이프라인 결과와 일치

## Phase 3 — 비용 시나리오 계산기

목표: 단독 입력의 최종 Token ID 기반 입력비 한 가지에서 실제 요청에 가까운 사용량 시나리오로 확장합니다.

범위:

- 입력·출력·캐시 읽기·캐시 쓰기 토큰 분리
- 호출당·일간·월간 요청 수와 예상 비용
- 긴 컨텍스트 티어, 프로모션 유효·최소 유지 기간, 통화 환산
- 추정값과 제공사 공식 계산 결과를 별도 상태로 표시

의존성·주의점:

- API 모델과 선택 토크나이저의 정확한 대응 정보
- 환율·가격 데이터의 기준 시각, 출처, 만료 처리
- 도구·검색·이미지·오디오·추론 토큰 같은 별도 과금은 독립 항목으로 분리

완료 조건:

- 티어 경계값 전후의 단가가 자동 테스트와 공식 표에 일치
- 모든 합계에 토큰 출처·단가 기준일·포함/제외 항목 표시
- 지원하지 않는 과금 요소를 0으로 오인할 수 없게 표시

## Phase 4 — 토크나이저 심층 탐색기

목표: 교육용 개요를 디버깅 가능한 토큰 단위 탐색으로 확장합니다.

범위:

- 원문 UTF-8 바이트, 코드 포인트, 정규화 전후 diff
- 각 토큰의 ID, raw token, 표시 문자열, byte 범위
- 특수 토큰 포함/제외 토글과 후처리 템플릿 표시
- vocab 검색과 선택 토큰 보고서

의존성·주의점:

- 정확한 원문 offset을 제공하지 않는 artifact에 대한 명시적 제한
- 정규화로 길이가 변할 때 원문과 결과 offset을 억지로 동일시하지 않기
- 매우 긴 입력에서 가상 스크롤 또는 렌더 상한 필요

완료 조건:

- 멀티바이트 문자와 결합 문자에서 깨진 대체 문자가 발생하지 않음
- offset 미지원 모델은 추정 매핑 대신 “지원 안 함”으로 표시
- 마우스 없이 토큰 상세 정보를 순서대로 탐색 가능

## 이후 검토 — 사용자 지정 Hugging Face artifact

사용자가 모델 ID와 revision을 입력해 토크나이저를 불러오는 기능은 가치가 크지만 가장 마지막에 검토합니다. CORS, gated 저장소, 라이선스, 임의 코드 신뢰, 파일 크기, 브라우저 메모리, revision 고정 정책을 먼저 설계해야 합니다.

## 착수 순서

1. Phase 1의 내보내기 스키마와 URL 상태 사양
2. Phase 2의 다중 로드 취소·메모리 정책
3. Phase 3의 과금 데이터 스키마와 공식 출처 갱신 절차
4. Phase 4의 offset 지원 가능성 조사
5. 사용자 지정 artifact 보안·리소스 검토
